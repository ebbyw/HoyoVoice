# Auto detect text files and perform LF normalization
* text=auto

# binary assets are never LF-normalized (no PNGs ship today — anchor
# templates self-calibrate locally — but crop artifacts and any future
# asset must not be mangled if one is ever added)
*.png binary
